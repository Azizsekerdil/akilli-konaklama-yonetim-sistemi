# ============================================================
#  Gelistirme / test / guvenlik bagimliliklari
#  Kurulum:  .\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
# ============================================================
-r requirements.txt

# --- Test ---
pytest>=8.3
pytest-cov>=6.0
pytest-qt>=4.4
pytest-asyncio>=0.24
pytest-mock>=3.14
freezegun>=1.5
respx>=0.21

# --- Kod kalitesi ---
ruff>=0.8
black>=24.10
mypy>=1.13
types-python-dateutil

# --- Guvenlik taramasi ---
bandit[toml]>=1.8
pip-audit>=2.7
detect-secrets>=1.5

# --- Paketleme ---
pyinstaller>=6.11

# --- Derleme araci ---
# Sanal ortamin onyuklenen setuptools surumu eski olabilir (Python 3.11'de
# venv 65.x kurar) ve bu seride bilinen aciklar vardir. Calisma zamani
# bagimliligi DEGILDIR; yalnizca derleme/paketleme ortamini guncel tutar.
setuptools>=83
